/**
 * Service worker reserved for later milestones (messaging, history, scripting).
 * Keep this file side-effect light; do not inspect page content here.
 */
chrome.runtime.onInstalled.addListener(() => {
  // Placeholder for future install/update hooks.
});
